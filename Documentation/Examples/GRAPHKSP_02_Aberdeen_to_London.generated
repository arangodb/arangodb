arangosh&gt; db._query(<span class="hljs-string">"FOR v, e IN OUTBOUND SHORTEST_PATH 'places/Aberdeen' TO 'places/London' GRAPH 'shortestPathsGraph' RETURN [v,e]"</span>);
[ArangoError <span class="hljs-number">1924</span>: AQL: graph <span class="hljs-string">'shortestPathsGraph'</span> not found (<span class="hljs-keyword">while</span> parsing)]
arangosh&gt; db._query(<span class="hljs-string">"FOR p IN OUTBOUND K_SHORTEST_PATHS 'places/Aberdeen' TO 'places/London' GRAPH 'shortestPathsGraph' LIMIT 1 RETURN p"</span>);
[ArangoError <span class="hljs-number">1924</span>: AQL: graph <span class="hljs-string">'shortestPathsGraph'</span> not found (<span class="hljs-keyword">while</span> instantiating plan)]
