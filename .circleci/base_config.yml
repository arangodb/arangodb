version: 2.1

orbs:
  aws-cli: circleci/aws-cli@4.1.1
  aws-ecr: circleci/aws-ecr@9.0.1
  # Exact pin: a floating orb range silently changes what runs. Keep in
  # sync with base_nightly_packages.yml.
  trivy-scan: arangodb/trivy-scan@1.2.0

parameters:
  enterprise-commit:
    type: string
    default: ""
  arangosh_args:
    type: string
    default: ""
  use-minimal-debug-info:
    type: boolean
    default: true
  extra_args:
    type: string
    default: ""
  build-docker-image:
    type: string
    default: "public.ecr.aws/b0b8h2r4/arangodb/ubuntubuildarangodb-40:24.04-dc3196a5"
    description: "Docker image to use for building ArangoDB - only relevant when trying new build images"
  test-docker-image:
    type: string
    default: public.ecr.aws/b0b8h2r4/test-ubuntu:24.04-d6a8996c8
  # Unused here, but it will be forwarded from config and will cause errors if not defined
  enterprise-branch:
    type: string
    default: ""
  dont-cancel-pipelines:
    type: boolean
    default: false
  create-test-docker-images:
    type: enum
    enum: [none, alpine]
    default: none
  rebuild-test-docker-images:
    type: boolean
    default: false
  full:
    type: boolean
    default: false
  replication-two:
    type: boolean
    default: false
  without-instrumentation:
    type: boolean
    default: true
  with-tsan:
    type: boolean
    default: false
  with-alubsan:
    type: boolean
    default: false
  c-compiler:
    type: string
    default: "clang"
  cxx-compiler:
    type: string
    default: "clang++"
  config-definitions:
    type: string
    default: ""
  filter-testsuite:
    type: string
    default: ""
  config-definitions-branches:
    type: string
    default: ""

  # these are basically global constants
  build-targets:
    type: string
    default: "arangodb"
    # The arangodb target consists of arangod and the client tools
    # (arangobackup is enterprise-only).
    # This should also improve build times because this way the individual targets can be
    # built in parallel (a list of individual targets is built serially).
  test-build-targets:
    type: string
    default: "arangodbtests fuertetest"
  # only used by the kafka driver tests
  kafkaHostList:
    type: string
    default: "kafka-1:19092,kafka-2:29092,kafka-3:39092"


.shared-test-job-parameters: &shared-test-job-parameters
  suiteName:
    type: string
  suites:
    type: string
  arangosh_args:
    type: string
    default: ""
  size:
    type: string
  extraArgs:
    type: string
    default: ""
  buckets:
    type: integer
    default: 1
  cluster:
    type: boolean
  timeLimit:
    type: integer
    default: 1800
  sanitizer:
    type: string
    default: ""
  docker_image:
    type: string
    default: "<< pipeline.parameters.test-docker-image >>"


commands:
  persist_workspace_to_s3:
    parameters:
      root:
        type: string
      paths:
        type: string
      archive:
        type: string
        default: workspace.tar.gz
    steps:
      - run:
          name: Save workspace to S3
          command: |
            set -eo pipefail
            set -x

            ROOT="<< parameters.root >>"
            PATHS="<< parameters.paths >>"
            ARCHIVE="<< parameters.archive >>"

            if [ -z "${AWS_S3_WORKSPACE_BUCKET:-}" ]; then
              echo "AWS_S3_WORKSPACE_BUCKET not set"
              exit 1
            fi

            mkdir -p /tmp/circleci-workspace
            TAR="/tmp/circleci-workspace/${ARCHIVE}"

            cd "$ROOT"

            tar -czf "$TAR" $PATHS
            du -sh "$TAR"
            aws configure set profile.anon.s3.multipart_threshold 5GB
            aws s3 cp --no-sign-request --profile anon \
              "$TAR" \
              "s3://${AWS_S3_WORKSPACE_BUCKET}/workspaces/${CIRCLE_WORKFLOW_WORKSPACE_ID}/${ARCHIVE}"

  attach_workspace_from_s3:
    parameters:
      at:
        type: string
        default: .
      archive:
        type: string
        default: workspace.tar.gz
    steps:
      - run:
          name: restore workspace from S3
          command: |
            set -eo pipefail
            set -x

            ARCHIVE="<< parameters.archive >>"

            if [ -z "${AWS_S3_WORKSPACE_BUCKET:-}" ]; then
              echo "AWS_S3_WORKSPACE_BUCKET not set"
              exit 1
            fi

            mkdir -p /tmp/circleci-workspace
            # TAR="/tmp/circleci-workspace/${ARCHIVE}"

            cd "<< parameters.at >>"

            aws configure set profile.anon.s3.multipart_threshold 5GB
            # aws s3 cp --no-sign-request --profile anon \
            #   "s3://${AWS_S3_WORKSPACE_BUCKET}/workspaces/${CIRCLE_WORKFLOW_WORKSPACE_ID}/${ARCHIVE}" \
            #   "$TAR"
            # tar --zstd -xf "$TAR"

            aws s3 cp --no-sign-request --profile anon \
              "s3://${AWS_S3_WORKSPACE_BUCKET}/workspaces/${CIRCLE_WORKFLOW_WORKSPACE_ID}/${ARCHIVE}" \
              - | tar -xzf -

  github-auth:
    # Anonymous clones from the runners' shared egress IP trip GitHub's limit on
    # unauthenticated downloads (CI outage 2026-09-02/03). The arango-ci-checkout
    # GitHub App (contents:read, public repos only) supplies a 1h token; without
    # credentials or on any failure the job clones anonymously, as before.
    # Always paired with github-auth-clear after the clone.
    steps:
      - run:
          name: Authenticate git against GitHub
          command: |
            set +x
            if git config --global --get http.https://github.com/.extraheader >/dev/null 2>&1; then
              exit 0
            fi
            if [ -z "${GITHUB_APP_ID:-}" ] || [ -z "${GITHUB_APP_PRIVATE_KEY_B64:-}" ]; then
              echo "WARNING: GITHUB_APP_ID / GITHUB_APP_PRIVATE_KEY_B64 not set, cloning anonymously" >&2
              exit 0
            fi
            KEY="$(mktemp)"
            trap 'rm -f "$KEY"' EXIT
            printf '%s' "$GITHUB_APP_PRIVATE_KEY_B64" | base64 -d > "$KEY"
            b64url() { openssl base64 -A | tr '+/' '-_' | tr -d '='; }
            NOW=$(date +%s)
            JWT="$(printf '{"alg":"RS256","typ":"JWT"}' | b64url).$(printf '{"iat":%d,"exp":%d,"iss":"%s"}' "$((NOW-60))" "$((NOW+540))" "$GITHUB_APP_ID" | b64url)"
            JWT="$JWT.$(printf '%s' "$JWT" | openssl dgst -sha256 -sign "$KEY" -binary | b64url)" || JWT=""
            INST=""
            TOKEN=""
            if [ -n "$JWT" ]; then
              INST=$(curl -sSf -H "Authorization: Bearer $JWT" -H "Accept: application/vnd.github+json" \
                       https://api.github.com/orgs/arangodb/installation | jq -r '.id // empty') || INST=""
            fi
            if [ -n "$INST" ]; then
              TOKEN=$(curl -sSf -X POST -H "Authorization: Bearer $JWT" -H "Accept: application/vnd.github+json" \
                        "https://api.github.com/app/installations/$INST/access_tokens" | jq -r '.token // empty') || TOKEN=""
            fi
            if [ -z "$TOKEN" ]; then
              echo "WARNING: no GitHub App token obtained, cloning anonymously" >&2
              exit 0
            fi
            git config --global http.https://github.com/.extraheader \
              "AUTHORIZATION: basic $(printf 'x-access-token:%s' "$TOKEN" | base64 | tr -d '\n')"
            echo "git authenticated against github.com as GitHub App installation $INST (token valid 1h)"

  github-auth-clear:
    # Drop the token header again once the clones it was set for are done, so a
    # job running past the token's hour never fails on an expired header where
    # an anonymous request would have worked.
    steps:
      - run:
          name: Forget GitHub token
          command: git config --global --unset-all http.https://github.com/.extraheader || true

  checkout-arangodb:
    parameters:
      destination:
        type: string
      with-submodules:
        type: boolean
    steps:
      - github-auth
      - run:
          name: Checkout ArangoDB
          command: |
            mkdir -p << parameters.destination >>
            cd << parameters.destination >>
            echo << pipeline.git.revision >>
            git init
            git remote add origin https://github.com/arangodb/arangodb.git
            echo "Fetching stuff"
            git fetch --depth 1 origin << pipeline.git.revision >>
            git checkout << pipeline.git.revision >>
      - when:
          condition: << parameters.with-submodules >>
          steps:
            - run:
                name: "Checkout submodules"
                command: |
                  cd << parameters.destination >>
                  git submodule init
                  git submodule update --recursive --depth 1 --jobs 8
      - github-auth-clear

  checkout-enterprise:
    description: "Checkout enterprise code"
    parameters:
      destination:
        type: string
    steps:
      - add_ssh_keys:
          fingerprints:
            - "f9:49:75:1a:ad:44:89:10:4b:3c:70:70:ba:d3:c3:ce"
      - run:
          name: Checkout Enterprise
          environment:
            GIT_SSH_COMMAND: "ssh -o StrictHostKeyChecking=no"
          command: |
            mkdir -p << parameters.destination >>
            cd << parameters.destination >>
            git clone git@github.com:arangodb/enterprise.git .
            git reset --hard << pipeline.parameters.enterprise-commit >>

  clone-driver-repo:
    parameters:
      driver-git-repo:
        type: string
      driver-git-branch:
        type: string
    steps:
      - run:
          name: Clone driver repository
          command: |
            cd ..
            export GIT_SSH_COMMAND="ssh -o StrictHostKeyChecking=no"
            git clone --single-branch --branch << parameters.driver-git-branch >> << parameters.driver-git-repo >>

  fetch-aux-binaries:
    parameters:
      arch:
        type: string
      repo-path:
        type: string
    steps:
      - run:
          name: Fetch aux binaries
          command: |
            set -x
            STARTER_REV=$(grep -Po "STARTER_REV \"\Kv\d+\.\d+\.\d+[a-z0-9-]*" <<parameters.repo-path>>/VERSIONS)
            RCLONE_GO=$(grep -Po "RCLONE_GO \"\K\d+\.\d+\.\d+\-[0-9a-h]+_[0-9]+" <<parameters.repo-path>>/VERSIONS)
            RCLONE_VERSION=$(grep -Po "RCLONE_VERSION \"\K\d+\.\d+\.\d+" <<parameters.repo-path>>/VERSIONS)
            ARANGO_MAJOR_MINOR_VERSION=$(grep -Po "\K\d+\.\d+" <<parameters.repo-path>>/ARANGO-VERSION)
            arch="<<parameters.arch>>"
            if test "$arch" == "x64"; then
              arch="amd64"
            elif test "$arch" == "aarch64"; then
              arch="arm64"
            fi
            curl -s -L -o <<parameters.repo-path>>/build/bin/arangodb "https://github.com/arangodb-helper/arangodb/releases/download/$STARTER_REV/arangodb-linux-$arch"
            chmod a+x <<parameters.repo-path>>/build/bin/arangodb

            curl -s -L -o <<parameters.repo-path>>/build/bin/rclone-arangodb "https://github.com/arangodb/rclone-arangodb/releases/download/golang-${RCLONE_GO}/golang-${RCLONE_GO}_${ARANGO_MAJOR_MINOR_VERSION}_v${RCLONE_VERSION}_rclone-arangodb-linux-$arch"
            chmod a+x <<parameters.repo-path>>/build/bin/rclone-arangodb
  run-test:
    parameters:
      suiteName:
        type: string
      suites:
        type: string
      cluster:
        type: boolean
      arangosh_args:
        type: string
      timeLimit:
        type: integer
      extraArgs:
        type: string
      sanitizer:
        type: string
      use-s3-workspace:
        type: boolean
        default: true
    steps:
      - add_ssh_keys:
          fingerprints:
            - "f9:49:75:1a:ad:44:89:10:4b:3c:70:70:ba:d3:c3:ce"
      - when:
          condition:
            not:
              << parameters.use-s3-workspace >>
          steps:
            - attach_workspace:
                at: .
      - when:
          condition:
            << parameters.use-s3-workspace >>
          steps:
            - attach_workspace_from_s3:
                at: .
                archive: binaries.tar.gz
      - run:
          name: Log processes
          command: |
            export PS_FORMAT="pid,%cpu,%mem,rss,thcount,oom,oomadj,args"
            ps | head -n 1
            echo "======"
            while true; do
              sleep 3
              ps -a | grep "[a]rangod " || true | while read line ; do echo "$(date +"%T") > $line" ; done ;
              echo "======"
            done
          background: true
      - when:
          condition:
            not:
              equal: [<< parameters.sanitizer >>, ""]
          steps:
            - run:
                name: Symbolizer server
                command: ./utils/llvm-symbolizer-server.py
                background: true
      - run:
          name: Run << parameters.suiteName >> tests
          # we increase the no_output_timeout so our own timeout mechanism can kick in and gather more information
          no_output_timeout: 20m
          command: |
            set -x
            # ulimit needs to be in here since it only has effect for this verry shell and its subprocesses:
            ulimit -c unlimited
            mkdir work
            if [ "<< parameters.sanitizer >>" = "alubsan" ]; then
              export ASAN_OPTIONS="log_exe_name=true:log_path=`pwd`/work/alubsan.log:handle_ioctl=true:check_initialization_order=true:detect_container_overflow=true:detect_stack_use_after_return=false:detect_odr_violation=1:strict_init_order=true:external_symbolizer_path=`pwd`/utils/llvm-symbolizer-client.py"
              export LSAN_OPTIONS="log_exe_name=true:log_path=`pwd`/work/alubsan.log:suppressions=lsan_arangodb_suppressions.txt:print_suppressions=0"
              export UBSAN_OPTIONS="log_exe_name=true:log_path=`pwd`/work/alubsan.log:print_stacktrace=1:suppressions=ubsan_arangodb_suppressions.txt:print_suppressions=0:external_symbolizer_path=`pwd`/utils/llvm-symbolizer-client.py"
              export SAN_MODE=alubsan
            elif [ "<< parameters.sanitizer >>" = "tsan" ]; then
              # Needed to ignore TSAN errors from OpenMP
              export OMP_TOOL_LIBRARIES=/usr/lib/llvm-19/lib/libarcher.so
              export ARCHER_OPTIONS="verbose=1"
              if ! [ -f ${OMP_TOOL_LIBRARIES} ]; then
                echo "File ${OMP_TOOL_LIBRARIES} does not exist."
                exit 1
              fi
              export TSAN_OPTIONS="log_exe_name=true:log_path=`pwd`/work/tsan.log:detect_deadlocks=true:second_deadlock_stack=1:suppressions=tsan_arangodb_suppressions.txt:print_suppressions=0:external_symbolizer_path=`pwd`/utils/llvm-symbolizer-client.py:ignore_noninstrumented_modules=1"
              export SAN_MODE=tsan
            fi

            if [ "$SAN_MODE" != "" ]; then
              # When running with sanitizer we have to LOWER the number of file descriptors!
              # The sanitizer forks off a separate process to handle the symbolization of stack traces,
              # and the fork operation iterates over all file descriptors, making this extremely expensive.
              ulimit -n 65536
            fi

            # check if promtool is available and if so set the environment variable to enable the according tests
            if command -v promtool &> /dev/null; then
              export PROMTOOL_PATH=`command -v promtool`
            fi
            export TIMELIMIT=<< parameters.timeLimit >>
            # Note: extraArgs can have references to env variables but also contain double quotes.
            # So we need to escape any double quotes, and then pass it through eval to resolve the env variables.
            RAW_EXTRA_ARGS='<< parameters.extraArgs >>'
            ESCAPED_RAW=${RAW_EXTRA_ARGS//\"/\\\"}
            eval "EXTRA_ARGS=\"$ESCAPED_RAW\""
            python3 -u scripts/test/test_launch_controller.py '<< parameters.suites >>' \
              --suiteName '<< parameters.suiteName >>' \
              --arangosh_args '<< parameters.arangosh_args >>' \
              --testBuckets $CIRCLE_NODE_TOTAL/$CIRCLE_NODE_INDEX \
              --cluster << parameters.cluster >> \
              --extraArgs "${EXTRA_ARGS}"
      - run:
          name: Copy test results
          when: always
          command: |
            ls -la ./work/
            mkdir test-results
            find testrunXml -iname *xml -exec cp "{}" --target-directory=./test-results \;
      - store_artifacts:
          destination: << parameters.suiteName >>
          path: ./work/
      - store_test_results:
          path: ./test-results/

jobs:
  minimal-checkout:
    docker:
      - image: cimg/base:current
    resource_class: small
    environment:
      GIT_SSH_COMMAND: ssh -v
    steps:
      - checkout-arangodb:
          with-submodules: false
          destination: "/home/circleci/project"
      - checkout-enterprise:
          destination: "/home/circleci/project/enterprise"
      - run:
          name: Checkout rta-makedata
          command: |
            # we are only interested in the rta-makedata submodule because we want to run eslint on it,
            # but we do not care about the other submodules (specifically we do _not_ want to clone v8)
            git submodule update --init 3rdParty/rta-makedata
      - persist_to_workspace:
          root: .
          paths: .

  check-log-ids:
    docker:
      - image: cimg/python:3.11.1
    resource_class: small
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Install pyyaml
          command: |
            python -m pip install --upgrade pip
            pip install pyyaml
      - run:
          name: Check LogIDs
          command: |
            python3 utils/checkLogIds.py

  check-metrics-docs:
    docker:
      - image: cimg/python:3.11.1
    resource_class: small
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Install pyyaml
          command: |
            python -m pip install --upgrade pip
            pip install pyyaml
      - run:
          name: Check LogIDs
          command: |
            python3 utils/generateAllMetricsDocumentation.py

  clang-format:
    docker:
      - image: alpine:3.15
    resource_class: small
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Install clang-format
          command: |
            apk add --no-cache git bash coreutils grep clang-extra-tools=12.0.1-r1
      - run:
          name: Print clang-format version
          command: clang-format --version
      - run:
          name: Run clang-format
          command: |
            scripts/clang-format-circleci.sh
      - run:
          name: Store diff
          command: |
            OUTCOME_BASE="/tmp/clang_format_outcome"
            OUTCOME_COMMUNITY=$OUTCOME_BASE/community.diff
            OUTCOME_ENTERPRISE=$OUTCOME_BASE/enterprise.diff
            mkdir -p $OUTCOME_BASE
            if [ -n "$(git status --porcelain)" ] ; then
                git diff | tee $OUTCOME_COMMUNITY
            fi
            if [ -n "$(git -C enterprise status --porcelain)" ] ; then
                git -C enterprise diff | tee $OUTCOME_ENTERPRISE
            fi
            if [ -f "$OUTCOME_COMMUNITY" ] || [ -f "$OUTCOME_ENTERPRISE" ]; then
              exit 1
            fi
      - store_artifacts:
          path: /tmp/clang_format_outcome

  eslint:
    docker:
      - image: alpine:3.17
    resource_class: small
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Install eslint
          command: |
            apk add --no-cache npm curl jq
            npm -g install eslint@8.46.0
      - run:
          name: Run eslint
          command: |
            utils/eslint.sh
      - run:
          name: "Cancel other workflows"
          when: on_fail
          command: |
            ## Get the IDs of currently running/on_hold workflows, but exclude ourselves
            curl --header "Circle-Token: $CIRCLECI_TOKEN" --request GET "https://circleci.com/api/v2/pipeline/${CIRCLE_PIPELINE_ID}/workflow" | \
                jq -r '.items[]|select(.id != "'${CIRCLE_WORKFLOW_ID}'" and (.status == "on_hold" or .status == "running")) | .id' >> WF_to_cancel.txt

            ## Cancel any currently running/on_hold workflow with the same name
            if [ -s WF_to_cancel.txt ]; then
              echo "Cancelling the following workflow(s):"
              cat WF_to_cancel.txt
              while read WF_ID;
                do
                  curl --header "Circle-Token: $CIRCLECI_TOKEN" --request POST https://circleci.com/api/v2/workflow/$WF_ID/cancel
                done < WF_to_cancel.txt
            else
              echo "Nothing to cancel"
            fi

  # PR-time lint of what governs the nightly security gates: disposition
  # hygiene and the required-status-check inventory.
  security-governance:
    docker:
      - image: cimg/base:current
    resource_class: small
    steps:
      - attach_workspace:
          at: .
      # No suppression files exist here today; this keeps any future one
      # dated and in-window. poam-windows matches the trivy-gate-waivers
      # table; nosem-exclude skips vendored trees.
      - trivy-scan/disposition-check:
          poam-windows: "critical=30,high=30,medium=90,low=180"
          nosem-exclude: ":!:.circleci/* :!:3rdParty/* :!:*/node_modules/*"
      - run:
          name: Compare required checks against the live configuration
          environment:
            # "true" anywhere in here marks a variant pipeline, which renames
            # the build workflows by design (generate_config.py appends the
            # variant to the workflow name).
            PIPELINE_VARIANTS: "<< pipeline.parameters.with-tsan >>,<< pipeline.parameters.with-alubsan >>,<< pipeline.parameters.full >>,<< pipeline.parameters.replication-two >>"
          command: |
            set -eu
            set -o pipefail
            LIST=.circleci/required-checks.txt
            # PR pipelines run on the head branch, so the protected branch
            # cannot be derived from CIRCLE_BRANCH.
            PROTECTED=4.0
            DRIFT=0
            if [ ! -s "${LIST}" ]; then
              echo "ERROR: ${LIST} is missing or empty. Commit the required-check inventory." >&2
              exit 1
            fi
            # || true: grep exits 1 on a comments-only list; that is reported
            # as an error on the next line, not as a pipefail abort here.
            grep -vE '^[[:space:]]*(#|$)' "${LIST}" | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//' | sort -u > /tmp/required-expected.txt || true
            if [ ! -s /tmp/required-expected.txt ]; then
              echo "ERROR: ${LIST} lists no contexts." >&2
              exit 1
            fi
            echo "Committed required contexts (${LIST}):"
            sed -e 's/^/  /' /tmp/required-expected.txt
            # Live GitHub side. Branch protection needs an administration:read
            # token, rulesets only a repo token, so both are consulted. No
            # token means UNVERIFIED, never "no drift".
            : > /tmp/required-live.txt
            LIVE_SOURCE=""
            PROT_OK=0
            if [ -n "${GITHUB_TOKEN:-}" ]; then
              GH_API="https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}"
              for ENDPOINT in "branches/${PROTECTED}/protection" "rules/branches/${PROTECTED}"; do
                if curl -sSfL --retry 3 --retry-delay 2 --max-time 60 \
                     -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                     -H "Accept: application/vnd.github+json" \
                     -H "X-GitHub-Api-Version: 2022-11-28" \
                     -o /tmp/gh-rules.json "${GH_API}/${ENDPOINT}"; then
                  # grep/sed: heredocs collide with CircleCI parameter
                  # syntax; "context" only appears in required checks.
                  grep -oE '"context":[[:space:]]*"[^"]*"' /tmp/gh-rules.json \
                    | sed -e 's/.*"\([^"]*\)"$/\1/' | sort -u >> /tmp/required-live.txt || true
                  LIVE_SOURCE="${LIVE_SOURCE}${LIVE_SOURCE:+, }${ENDPOINT}"
                  if [ "${ENDPOINT}" = "branches/${PROTECTED}/protection" ]; then
                    PROT_OK=1
                  fi
                else
                  echo "NOTICE: could not read ${ENDPOINT} (token scope?), that surface is unverified on this run." >&2
                fi
              done
            elif [ -n "${CIRCLE_PR_REPONAME:-}" ]; then
              echo "NOTICE: forked PR, no GITHUB_TOKEN; the live GitHub configuration is unverified on this run." >&2
            else
              echo "NOTICE: GITHUB_TOKEN is not set; the live GitHub configuration stays UNVERIFIED until a context with a read-only administration:read token is attached." >&2
            fi
            # Compare only when a surface that can carry required contexts
            # answered: an empty read off the rulesets endpoint alone is a
            # token-scope gap, not proof that nothing is required.
            if [ -n "${LIVE_SOURCE}" ] && { [ "${PROT_OK}" = "1" ] || [ -s /tmp/required-live.txt ]; }; then
              sort -u /tmp/required-live.txt -o /tmp/required-live.txt
              echo "Live required contexts for ${PROTECTED} (${LIVE_SOURCE}):"
              sed -e 's/^/  /' /tmp/required-live.txt
              MISSING="$(comm -23 /tmp/required-expected.txt /tmp/required-live.txt)" || true
              EXTRA="$(comm -13 /tmp/required-expected.txt /tmp/required-live.txt)" || true
              PARTIAL=0
              if [ -n "${MISSING}" ]; then
                # Only branch protection proves absence; a rulesets-only
                # read may be a partial view of the live configuration.
                if [ "${PROT_OK}" = "1" ]; then
                  echo "DRIFT: committed but NOT required by GitHub, so these checks do not block a merge:" >&2
                  printf '%s\n' "${MISSING}" | sed -e 's/^/  /' >&2
                  DRIFT=1
                else
                  echo "NOTICE: branch protection was unreadable, so it is unknown whether these committed contexts are live:" >&2
                  printf '%s\n' "${MISSING}" | sed -e 's/^/  /' >&2
                  PARTIAL=1
                fi
              fi
              if [ -n "${EXTRA}" ]; then
                echo "DRIFT: required by GitHub but NOT in the committed list:" >&2
                printf '%s\n' "${EXTRA}" | sed -e 's/^/  /' >&2
                DRIFT=1
              fi
              if [ "${DRIFT}" = "0" ] && [ "${PARTIAL}" = "0" ]; then
                echo "GitHub configuration matches the committed list."
              fi
            elif [ -n "${LIVE_SOURCE}" ]; then
              echo "NOTICE: the readable surfaces carried no required-context data; merge-gate drift is unverified on this run." >&2
            fi
            # Emitted side, read off this live pipeline: GitHub sees one
            # check per CircleCI WORKFLOW, and deriving the names from the
            # config instead is how they go stale.
            if [ -z "${CIRCLECI_TOKEN:-}" ]; then
              echo "NOTICE: CIRCLECI_TOKEN is not set; emitted workflow names are unverified on this run." >&2
            elif ! curl -sSfL --retry 3 --retry-delay 2 --max-time 60 \
                 -H "Circle-Token: ${CIRCLECI_TOKEN}" \
                 -o /tmp/wf.json \
                 "https://circleci.com/api/v2/pipeline/${CIRCLE_PIPELINE_ID}/workflow"; then
              echo "NOTICE: could not list this pipeline's workflows; the emitted-context check is unverified on this run." >&2
            else
              grep -oE '"name"[[:space:]]*:[[:space:]]*"[^"]*"' /tmp/wf.json \
                | sed -e 's/.*"\([^"]*\)"$/\1/' | sort -u > /tmp/required-emitted.txt || true
              echo "Workflows emitted by this pipeline (${CIRCLE_PIPELINE_ID}):"
              sed -e 's/^/  /' /tmp/required-emitted.txt
              FOUND=0
              while IFS= read -r CTX; do
                if grep -qxF -- "${CTX}" /tmp/required-emitted.txt; then
                  FOUND=$((FOUND + 1))
                fi
              done < /tmp/required-expected.txt
              if [ "${FOUND}" = "0" ]; then
                # Variant and devel pipelines legitimately emit other names;
                # a default head-branch pipeline emitting none means the
                # workflows were renamed, which blocks every merge.
                case "${PIPELINE_VARIANTS:-}" in *true*) VARIANT=1 ;; *) VARIANT=0 ;; esac
                if [ "${VARIANT}" = "1" ] || [ "${CIRCLE_BRANCH:-}" = "${PROTECTED}" ]; then
                  echo "NOTICE: variant or ${PROTECTED} pipeline; none of the committed contexts is emitted here, nothing to compare." >&2
                else
                  echo "DRIFT: this default pipeline emits none of the committed contexts; if the workflows were renamed, every merge into ${PROTECTED} is blocked by checks nothing emits." >&2
                  DRIFT=1
                fi
              else
                while IFS= read -r CTX; do
                  if ! grep -qxF -- "${CTX}" /tmp/required-emitted.txt; then
                    echo "DRIFT: required context '${CTX}' is not emitted by this pipeline; a required check nothing emits blocks every merge." >&2
                    DRIFT=1
                  fi
                done < /tmp/required-expected.txt
                [ "${DRIFT}" = "0" ] && echo "Every committed context is emitted by this pipeline."
              fi
            fi
            # Drift fails the job (lint is not a required check, so it
            # cannot block a merge); missing tokens only skip their half,
            # keeping forked PRs green.
            if [ "${DRIFT}" != "0" ]; then
              echo "ERROR: drift reported above. Fix the committed list or the branch protection." >&2
              exit 1
            fi

  compile-linux:
    parameters:
      preset:
        type: string
      enterprise:
        type: boolean
      build-tests:
        type: boolean
        default: true
      publish-artifacts:
        type: boolean
        default: true
      # Fetch aux binaries and produce install-<arch>.tar.gz plus the
      # per-arch docker build context for Docker image creation. Set only
      # by the docker-images workflow's compile jobs.
      create-install-package:
        type: boolean
        default: false
      resource-class:
        type: string
      s3-prefix:
        type: string
        default: ""
      arch:
        type: string
    docker:
      - image: << pipeline.parameters.build-docker-image >>
    resource_class: << parameters.resource-class >>
    environment:
      GIT_SSH_COMMAND: ssh
      SCCACHE_ERROR_LOG: /tmp/sccache.log
      SCCACHE_LOG: info,sccache::cache=debug,sccache::compiler::compiler=debug
      SCCACHE_S3_KEY_PREFIX: << parameters.s3-prefix >>
      CC: /tools/clang
      CXX: /tools/clang++
    steps:
      - checkout-arangodb:
          with-submodules: true
          destination: "/root/project"
      - when:
          condition: << parameters.enterprise >>
          steps:
            - checkout-enterprise:
                destination: "/root/project/enterprise"
      - run:
          name: Print SCCache Settings
          command: sccache -s
      - run:
          name: Configure
          command: |
            MINIMAL_DEBUGINFO="OFF"
            if [ << pipeline.parameters.use-minimal-debug-info >> = true ]; then
              MINIMAL_DEBUGINFO="ON"
            fi
            cmake --preset << parameters.preset >> \
              -G Ninja \
              -DCMAKE_C_COMPILER=<< pipeline.parameters.c-compiler >> \
              -DCMAKE_CXX_COMPILER=<< pipeline.parameters.cxx-compiler >> \
              -DCMAKE_LIBRARY_PATH=$OPENSSL_ROOT_DIR/lib \
              -DCMAKE_C_COMPILER_LAUNCHER=sccache \
              -DCMAKE_CXX_COMPILER_LAUNCHER=sccache \
              -DOPENSSL_ROOT_DIR=/opt \
              -DUSE_MINIMAL_DEBUGINFO=$MINIMAL_DEBUGINFO \
              -DCMAKE_INSTALL_PREFIX=/
      - run:
          name: Build
          command: |
            TARGETS="<< pipeline.parameters.build-targets >>"
            if [ << parameters.build-tests >> = true ]; then
              TARGETS="$TARGETS << pipeline.parameters.test-build-targets >>"
            fi
            echo "Building targets: $TARGETS"
            cmake --build --preset << parameters.preset >> --parallel 16 --target $TARGETS
      - when:
          condition: << parameters.create-install-package >>
          steps:
            - fetch-aux-binaries:
                arch: << parameters.arch >>
                repo-path: .
            - run:
                name: Create install package
                command: |
                  mkdir install
                  DESTDIR=`pwd`/install cmake --build --preset << parameters.preset >> --parallel 16 --target install
                  cp build/bin/arangodb ./install/usr/bin/
                  if [ << parameters.enterprise >> = true ]; then
                    # Same location as the nightly images (fetch-aux-binaries.sh):
                    # usr/sbin, so the client prune drops it and consumers find
                    # it where the published images have it.
                    cp build/bin/rclone-arangodb ./install/usr/sbin/
                  fi
                  # Strip per the packaging policy (client tools only; the
                  # starter and arangod keep their symbols).
                  if [ << parameters.enterprise >> = true ]; then EDITION=enterprise; else EDITION=community; fi
                  EDITION="${EDITION}" scripts/packaging/strip-install-tree.sh ./install
                  tar -czf install-<< parameters.arch >>.tar.gz -C install ./
                  cp -a scripts/docker/deploy docker-context-<< parameters.arch >>
            # Everything persisted here must be arch-specific: the x64 and
            # aarch64 compile jobs run concurrently, and concurrent jobs
            # persisting the same workspace path cause a workspace layer
            # conflict on attach. The docker build context (dockerfile,
            # setup.sh, entrypoint.sh) is copied per arch for the same
            # reason - each build-docker-image job requires only its own
            # arch's compile job, so it cannot rely on the other arch's
            # workspace layer being present.
            - persist_to_workspace:
                root: .
                paths:
                  - install-<< parameters.arch >>.tar.gz
                  - docker-context-<< parameters.arch >>
      - run:
          name: Size leaderboard
          command: |
            ls -Ssha build/bin/*.a
      - run:
          name: Cleanup build directory
          command: |
            find build/ -iname *.a -delete || true
            find build/ -iname *.o -delete || true
            find build/ -iname *.o.d -delete || true
      - run:
          name: List build artifacts
          command: |
            ls -Ssha build/bin/
      - run:
          name: SCCache Statistics
          command: sccache -s
      - store_artifacts:
          path: /tmp/sccache.log
      - when:
          condition: << parameters.publish-artifacts >>
          steps:
            - store_artifacts:
                path: build/bin/arango*
            - persist_to_workspace:
                root: .
                paths:
                  - VERSIONS
                  - ARANGO-VERSION
                  - build/bin
                  - build/etc
                  - scripts/
                  - js/
                  - enterprise/js
                  - etc/
                  - tests/js
                  - enterprise/tests/js
                  - utils
                  - lib/iresearch/tests/resources
                  - 3rdParty/rta-makedata
                  - tsan_arangodb_suppressions.txt
                  - ubsan_arangodb_suppressions.txt
                  - lsan_arangodb_suppressions.txt
            - persist_workspace_to_s3:
                root: .
                archive: cppcheck.tar.gz
                paths: |
                  build/compile_commands.json
            - persist_workspace_to_s3:
                # not there? install.tar.gz
                root: .
                archive: binaries.tar.gz
                paths: |
                  VERSIONS
                  ARANGO-VERSION
                  build/bin
                  build/etc
                  scripts/
                  js/
                  enterprise/js
                  etc/
                  tests/js
                  enterprise/tests/js
                  utils
                  lib/iresearch/tests/resources
                  3rdParty/rta-makedata
                  tsan_arangodb_suppressions.txt
                  ubsan_arangodb_suppressions.txt
                  lsan_arangodb_suppressions.txt

  # Builds one architecture's Alpine-based image locally and saves it to a
  # workspace tarball. No registry auth here - pushing (and the ECR login)
  # happens once both architectures are available together, in
  # push-docker-manifest. CircleCI workflows can't depend on jobs in
  # another workflow, so both architectures live in the same
  # "docker-images" workflow and hand off through the workspace.
  build-docker-image:
    parameters:
      resource-class:
        type: string
      # Docker platform architecture (amd64 | arm64), used for --platform
      # and the image's arch build-arg
      arch:
        type: string
      # Compile-side architecture (x64 | aarch64): selects the arch-specific
      # install tarball and docker build context persisted by the matching
      # compile-linux job
      build-arch:
        type: string
    docker:
      - image: cimg/base:current
    resource_class: << parameters.resource-class >>
    steps:
      - setup_remote_docker:
          docker_layer_caching: true
      - attach_workspace:
          at: .
      - run:
          name: Build Docker Images
          command: |
            # Two Alpine-based (3.24) test images per architecture:
            # core-test (server only, no client tools) and
            # client-tools-test (client tools only, pruned from the same
            # install tree the way build-targz.sh builds the client bundle).
            set -eu
            cd docker-context-<< parameters.build-arch >>
            mkdir install
            tar -xzf ../install-<< parameters.build-arch >>.tar.gz -C install
            cp -a install install-client
            rm -rf install-client/usr/sbin \
                   install-client/usr/bin/arangodb \
                   install-client/usr/share/arangodb4/js/server \
                   install-client/usr/share/arangodb4/arangodb-update-db \
                   install-client/var
            # Keep the client-tool configs in /etc/arangodb4; drop only the
            # server-side ones.
            rm -f install-client/etc/arangodb4/arangod.conf \
                  install-client/etc/arangodb4/arango-init-database.conf \
                  install-client/etc/arangodb4/arango-secure-installation.conf
            # Server image ships no client tools (mirrors the TAR.GZ
            # server bundle): drop the binaries, their configs, man pages
            # and the client-only JS tree.
            for tool in arangobackup arangodump arangoexport arangoimport \
                        arangoinspect arangorestore arangosh arangovpack; do
              rm -f install/usr/bin/"${tool}" \
                    install/etc/arangodb4/"${tool}".conf
            done
            rm -rf install/usr/share/man/man1 \
                   install/usr/share/arangodb4/js

            docker build --platform linux/<< parameters.arch >> --build-arg arch=<< parameters.arch >> -t arangodb/core-test:local-<< parameters.arch >> --file deploy-alpine.dockerfile .
            docker save -o /tmp/docker-core-<< parameters.build-arch >>.tar arangodb/core-test:local-<< parameters.arch >>
            docker build --platform linux/<< parameters.arch >> --build-arg arch=<< parameters.arch >> -t arangodb/client-tools-test:local-<< parameters.arch >> --file deploy-alpine-client.dockerfile .
            docker save -o /tmp/docker-client-tools-<< parameters.build-arch >>.tar arangodb/client-tools-test:local-<< parameters.arch >>
      - persist_to_workspace:
          root: /tmp
          paths:
            - docker-core-<< parameters.build-arch >>.tar
            - docker-client-tools-<< parameters.build-arch >>.tar

  # Loads both architectures' locally-built images (two image names each:
  # core-test and client-tools-test), pushes each as an arch-suffixed tag,
  # then creates and pushes one multi-arch manifest per image name under
  # the bare tag (<community sha7>_<enterprise sha7>). ECR only, using the
  # same OIDC role assumption as before - no stored credentials/context.
  push-docker-manifest:
    parameters:
      resource-class:
        type: string
      tag:
        type: string
    docker:
      - image: cimg/base:current
    resource_class: << parameters.resource-class >>
    steps:
      - aws-cli/setup:
          role_arn: "arn:aws:iam::242063586340:role/CircleCI-Container-Handler"
      - aws-ecr/ecr_login:
          public_registry: true
      - setup_remote_docker:
          docker_layer_caching: true
      - attach_workspace:
          at: /tmp/workspace
    # build-arch tarball names (x64/aarch64) come from the compile-side
    # architecture; the docker-side arch pair is amd64/arm64.
      - run:
          name: Push multi-arch Docker manifests
          command: |
            declare -A BUILD_ARCH=( [amd64]="x64" [arm64]="aarch64" )
            declare -A TAG_ARCH=( [amd64]="amd64" [arm64]="arm64v8" )

            for NAME in core client-tools; do
              for ARCH in amd64 arm64; do
                docker load -i /tmp/workspace/docker-${NAME}-${BUILD_ARCH[$ARCH]}.tar
              done
            done

            set -x
            retry() { "$@" || { sleep 5; "$@"; } || { sleep 15; "$@"; } || { sleep 30; "$@"; }; }

            for NAME in core client-tools; do
              IMAGE="public.ecr.aws/b0b8h2r4/arangodb/${NAME}-test"
              for ARCH in amd64 arm64; do
                docker tag "arangodb/${NAME}-test:local-${ARCH}" "${IMAGE}:<< parameters.tag >>-${TAG_ARCH[$ARCH]}"
                retry docker push "${IMAGE}:<< parameters.tag >>-${TAG_ARCH[$ARCH]}"
              done
              docker manifest rm "${IMAGE}:<< parameters.tag >>" 2>/dev/null || true
              retry docker manifest create "${IMAGE}:<< parameters.tag >>" \
                --amend "${IMAGE}:<< parameters.tag >>-amd64" \
                --amend "${IMAGE}:<< parameters.tag >>-arm64v8"
              retry docker manifest push --purge "${IMAGE}:<< parameters.tag >>"
            done

  run-linux-tests:
    parameters:
      <<: *shared-test-job-parameters
    docker:
      - image: << parameters.docker_image >>
    resource_class: << parameters.size >>
    parallelism: << parameters.buckets >>
    steps:
      - run-test:
          suiteName: << parameters.suiteName >>
          suites: << parameters.suites >>
          cluster: << parameters.cluster >>
          arangosh_args: << parameters.arangosh_args >>
          timeLimit: << parameters.timeLimit >>
          sanitizer: << parameters.sanitizer >>
          extraArgs: << parameters.extraArgs >>

  run-linux-tests-with-smocker:
    parameters:
      <<: *shared-test-job-parameters
    docker:
      - image: << parameters.docker_image >>
      - image: ghcr.io/smocker-dev/smocker
        name: smocker
    resource_class: << parameters.size >>
    parallelism: << parameters.buckets >>
    environment:
      SMOCKER_HOST: smocker
    steps:
      - run-test:
          suiteName: << parameters.suiteName >>
          suites: << parameters.suites >>
          cluster: << parameters.cluster >>
          arangosh_args: << parameters.arangosh_args >>
          timeLimit: << parameters.timeLimit >>
          sanitizer: << parameters.sanitizer >>
          extraArgs: << parameters.extraArgs >>

  run-driver-tests:
    parameters:
      <<: *shared-test-job-parameters
      driver-git-repo:
        type: string
      driver-git-branch:
        type: string
      init_command:
        type: string
        default: ""
    docker:
      - image: << parameters.docker_image >>
    resource_class: << parameters.size >>
    parallelism: << parameters.buckets >>
    steps:
      - clone-driver-repo:
          driver-git-repo: << parameters.driver-git-repo >>
          driver-git-branch: << parameters.driver-git-branch >>
      - run:
          name: Initialize driver tests
          command: |
            cd ../$(echo << parameters.driver-git-repo >>|sed -e "s;.*/;;" -e "s;.git;;" )
            << parameters.init_command >>
      - run-test:
          suiteName: << parameters.suiteName >>
          suites: << parameters.suites >>
          cluster: << parameters.cluster >>
          arangosh_args: << parameters.arangosh_args >>
          timeLimit: << parameters.timeLimit >>
          sanitizer: << parameters.sanitizer >>
          extraArgs: << parameters.extraArgs >>

  kafka-driver-tests:
    parameters:
      <<: *shared-test-job-parameters
      driver-git-repo:
        type: string
      driver-git-branch:
        type: string
    docker:
      - image: << parameters.docker_image >>
      - image: docker.io/bitnamilegacy/kafka:3.9
        name: kafka-1
        environment:
          - BITNAMI_DEBUG: true
          - ALLOW_PLAINTEXT_LISTENER: "yes"
          - KAFKA_CFG_NODE_ID: 1
          - KAFKA_CFG_PROCESS_ROLES: controller,broker
          - KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,PLAINTEXT_HOST://0.0.0.0:19092,CONTROLLER://:9093
          - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: "1@kafka-1:9093,2@kafka-2:9093,3@kafka-3:9093"
          - KAFKA_KRAFT_CLUSTER_ID: abcdefghijklmnopqrstuv
          - KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
          - KAFKA_CFG_ADVERTISED_LISTENERS: "PLAINTEXT://kafka-1:9092,PLAINTEXT_HOST://kafka-1:19092"
          - KAFKA_CFG_INTER_BROKER_LISTENER_NAME: PLAINTEXT
          - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT,CONTROLLER:PLAINTEXT
      - image: docker.io/bitnamilegacy/kafka:3.9
        name: kafka-2
        environment:
          - BITNAMI_DEBUG: true
          - ALLOW_PLAINTEXT_LISTENER: "yes"
          - KAFKA_CFG_NODE_ID: 2
          - KAFKA_CFG_PROCESS_ROLES: controller,broker
          - KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,PLAINTEXT_HOST://0.0.0.0:29092,CONTROLLER://:9093
          - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: "1@kafka-1:9093,2@kafka-2:9093,3@kafka-3:9093"
          - KAFKA_KRAFT_CLUSTER_ID: abcdefghijklmnopqrstuv
          - KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
          - KAFKA_CFG_ADVERTISED_LISTENERS: "PLAINTEXT://kafka-2:9092,PLAINTEXT_HOST://kafka-2:29092"
          - KAFKA_CFG_INTER_BROKER_LISTENER_NAME: PLAINTEXT
          - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT,CONTROLLER:PLAINTEXT
      - image: docker.io/bitnamilegacy/kafka:3.9
        name: kafka-3
        environment:
          - BITNAMI_DEBUG: true
          - ALLOW_PLAINTEXT_LISTENER: "yes"
          - KAFKA_CFG_NODE_ID: 3
          - KAFKA_CFG_PROCESS_ROLES: controller,broker
          - KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,PLAINTEXT_HOST://0.0.0.0:39092,CONTROLLER://:9093
          - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: "1@kafka-1:9093,2@kafka-2:9093,3@kafka-3:9093"
          - KAFKA_KRAFT_CLUSTER_ID: abcdefghijklmnopqrstuv
          - KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
          - KAFKA_CFG_ADVERTISED_LISTENERS: "PLAINTEXT://kafka-3:9092,PLAINTEXT_HOST://kafka-3:39092"
          - KAFKA_CFG_INTER_BROKER_LISTENER_NAME: PLAINTEXT
          - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT,CONTROLLER:PLAINTEXT
      - image: docker.io/confluentinc/cp-schema-registry:7.5.2
        name: kafka-schema-registry
        environment:
          - SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS=PLAINTEXT://kafka-1:19092,PLAINTEXT://kafka-2:29092,PLAINTEXT://kafka-3:39092
          - SCHEMA_REGISTRY_HOST_NAME=kafka-schema-registry
    resource_class: << parameters.size >>
    parallelism: << parameters.buckets >>
    environment:
      - KAFKA_HOST_LIST=kafka-1:19092,kafka-2:29092,kafka-3:39092
      - KAFKA_SCHEMA_HOST=http://kafka-schema-registry:8081
    steps:
      - clone-driver-repo:
          driver-git-repo: << parameters.driver-git-repo >>
          driver-git-branch: << parameters.driver-git-branch >>
      - run:
          name: Wait for Kafka brokers to be ready
          command: |
            count=0
            wait_server() {
                # shellcheck disable=SC2091
                until $(/usr/bin/nc -z $1 $2); do
                    count=$((count + 1))
                    if test "$count" -gt 300; then
                       print "server not ready after 5 mins, giving up."
                       exit 1
                    fi
                    printf '.'
                    sleep 1
                done
            }
            wait_server kafka-1 9092
            wait_server kafka-2 9092
            wait_server kafka-3 9092
            wait_server kafka-schema-registry 8081
      - run-test:
          suiteName: << parameters.suiteName >>
          suites: << parameters.suites >>
          cluster: << parameters.cluster >>
          arangosh_args: << parameters.arangosh_args >>
          timeLimit: << parameters.timeLimit >>
          sanitizer: << parameters.sanitizer >>
          extraArgs: << parameters.extraArgs >>
          use-s3-workspace: false

  run-cppcheck:
    docker:
      - image: << pipeline.parameters.build-docker-image >>
    resource_class: arangodb/medium-amd64
    steps:
      - run:
          name: Prepare container
          command: |
            mkdir -p /root/workspace/
            mkdir -p /root/project/
      - attach_workspace_from_s3:
          at: /root/workspace/
          archive: cppcheck.tar.gz
      - run:
          name: Print workspace size
          command: |
            du -sh /root/workspace/*
            echo ===================
            du -sh /root/workspace/build/*
      - checkout-arangodb:
          with-submodules: true
          destination: "/root/project"
      - checkout-enterprise:
          destination: "/root/project/enterprise"
      - run:
          name: Run cppcheck
          command: |
            ln -s /root/workspace/build/ /root/project/
            cd /root/project/
            utils/cppcheck-circleci.sh /root/workspace/build/compile_commands.json
      - run:
          name: Format result
          when: always
          command: |
            cd /root/project/
            sed -i 's/\/root\/project\/\/root\/project\///g' cppcheck.xml
            sed -i 's/\/root\/project\///g' cppcheck.xml
            cppcheck_junit cppcheck.xml cppcheck-junit.xml
      - store_artifacts:
          path: /root/project/cppcheck.xml
      - store_test_results:
          path: /root/project/cppcheck-junit.xml

workflows:
  lint:
    jobs:
      - minimal-checkout:
          name: minimal-checkout
      - check-log-ids:
          name: check-log-ids
          requires:
            - minimal-checkout
      - check-metrics-docs:
          name: check-metrics-docs
          requires:
            - minimal-checkout
      - clang-format:
          name: clang-format
          requires:
            - minimal-checkout
      - eslint:
          name: eslint
          context: [circleci-token]
          requires:
            - minimal-checkout
      - security-governance:
          name: security-governance
          context: [circleci-token]
          requires:
            - minimal-checkout
# all other workflows are defined in generate_config.py
