# Required status checks for merging into devel, one context per line. The
# security-governance job compares this committed expectation against the
# live GitHub branch-protection/ruleset configuration and against the
# workflows a real pipeline emitted: a required context nothing emits blocks
# every merge, one that silently matches a different workflow protects
# nothing, and neither is visible unless something compares the two.
#
# Read off the live configuration and confirmed against a real PR pipeline;
# do not hand-derive. GitHub sees ONE check per CircleCI workflow (not per
# job), so these are workflow names.
#
# The Trivy gates live in the nightly-packages pipeline, so they cannot be
# required checks on devel; they gate the publish through publish-nightly's
# requires: edges instead.

aarch64-pr
x64-pr
