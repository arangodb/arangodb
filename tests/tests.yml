- shell_client:
    options:
      priority: 250
      parallelity: 2
      buckets: 2
      type: single

- shell_client:
    options:
      priority: 500
      type: cluster
      size: small
      buckets: 5
    args:
      dumpAgencyOnError: true
