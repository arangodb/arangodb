test:
        name: server_parameters

        weight: 500

        options:
                - dumpAgencyOnError: true

        required:
                - mode:
                        - single-server
                        - cluster

        runs:
                - PR
                - Full
