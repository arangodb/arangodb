- jobProperties:
    container_suffix: -js:
    second_repo: git@github.com:arangodb/arangojs.git
    branch: main
    init_command: yarn

- js_driver:
    options:
      priority: 500
      type: single
- js_driver:
    options:
      priority: 500
      type: cluster
      size: large
