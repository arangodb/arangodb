jobProperties:
    container_suffix: '-spark:'
    second_repo: git@github.com:arangodb/arangodb-spark-datasource.git
    branch: main
    init_command: ''
tests:
- spark_datasource:
    options:
      priority: 500
      type: single
    args:
      extremeVerbosity: true
- spark_datasource:
    options:
      priority: 500
      type: cluster
    args:
      extremeVerbosity: true
