# {"container_suffix": "_go:", "second_repo": "git@github.com:arangodb/go-driver.git"}
# test_suite [flags ...] -- [additional-params...]
# for possible flags and params see
# https://github.com/arangodb/oskar#testing


# Single Server Replication Tests
go_driver priority=500 single -- --gosource ../go-driver 
go_driver priority=500 cluster -- --gosource ../go-driver 
