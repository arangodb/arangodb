# {"container_suffix": "-go:", "second_repo": "git@github.com:arangodb/go-driver.git", "branch": "master", "init_command":""}
# test_suite [flags ...] -- [additional-params...]
# for possible flags and params see
# https://github.com/arangodb/oskar#testing


# Single Server Replication Tests
go_driver priority=500 single size=medium suffix=v1 -- --goDriverVersion 1
go_driver priority=500 cluster suffix=v1 -- --goDriverVersion 1
go_driver priority=500 single size=large suffix=v2 -- --goDriverVersion 2
go_driver priority=500 cluster size=large suffix=v2 -- --goDriverVersion 2
