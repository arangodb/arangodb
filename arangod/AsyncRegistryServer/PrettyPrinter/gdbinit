# Use this file as example code for your ~/.gdbinit file.
python
import sys
# enter the correct path here, probably an absolute one.
# The relative path is used by the flex_vector_test.
sys.path.insert(0, './arangod/AsyncRegistryServer/PrettyPrinter/asyncregistry/src/')
# from asyncregistry.printer import register_printer
# register_printer()
end
