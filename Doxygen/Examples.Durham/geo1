FROM users u
WITHIN d = ( [ users.x, users.y ], [ 10.3, 40.5 ], 10000 )

FROM users u
WITHIN d = ( users.x, users.y , 10.3, 40.5 , 10000 )

FROM users u
WITHIN d = ( [ users.x, users.y ], [ 10.3, 40.5 ], 25 )

FROM users u
WITHIN d = ( users.x, users.y, 10.3, 40.5, 25 )
